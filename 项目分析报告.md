# 微人事(VHR)项目分析报告

## 📋 项目概述

**微人事(vhr)**是一个基于前后端分离架构的人力资源管理系统,由江南一点雨开发。该系统采用现代化的技术栈,提供了完整的员工管理、薪资管理、权限管理等功能模块,适合企业级应用场景。

---

## 🏗️ 项目结构

```
vhr-master/
├── vhr/                    # 后端项目
│   ├── vhrserver/         # 主服务模块
│   │   ├── vhr-model/     # 实体类模块
│   │   ├── vhr-mapper/    # 数据访问层
│   │   ├── vhr-service/   # 业务逻辑层
│   │   └── vhr-web/       # Web控制层
│   └── mailserver/        # 邮件服务模块
├── vuehr/                  # 前端项目(Vue.js)
│   ├── src/
│   │   ├── views/         # 页面组件
│   │   ├── components/    # 公共组件
│   │   ├── utils/         # 工具类
│   │   ├── store/         # Vuex状态管理
│   │   └── router.js      # 路由配置
│   └── dist/              # 打包后的静态文件
└── vhr.sql                # 数据库脚本
```

### 后端模块说明

| 模块 | 说明 | 负责内容 |
|------|------|---------|
| **vhr-model** | 实体类模块 | 定义数据库表对应的Java实体类(如Employee、Department、Salary等) |
| **vhr-mapper** | 数据访问层 | MyBatis Mapper接口,负责数据库CRUD操作 |
| **vhr-service** | 业务逻辑层 | 处理业务逻辑,调用Mapper层,实现事务管理 |
| **vhr-web** | Web控制层 | Spring Boot启动类、Controller、配置类、安全配置 |
| **mailserver** | 邮件服务模块 | 独立的邮件发送服务,接收RabbitMQ消息并发送邮件 |

---

## 💻 技术框架

### 后端技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| **Spring Boot** | 2.4.0 | 核心框架,提供自动配置、快速开发能力,简化Spring应用搭建 |
| **MyBatis** | - | 持久层框架,通过XML或注解配置SQL映射,灵活控制SQL执行 |
| **MySQL** | - | 关系型数据库,存储系统所有数据 |
| **Spring Security** | - | 安全框架,提供认证(登录验证)和授权(权限控制)功能 |
| **WebSocket** | - | 实时通信协议,实现在线聊天功能,支持双向消息推送 |
| **RabbitMQ** | - | 消息队列,异步处理邮件发送任务,解耦业务逻辑 |
| **Redis** | - | 内存数据库,缓存菜单数据,提升系统响应速度 |
| **Druid** | - | Alibaba数据库连接池,提供监控、SQL分析等功能 |
| **FastDFS** | - | 分布式文件系统,用于员工头像等文件的上传和存储 |
| **POI** | - | Apache POI,处理Excel文件的导入导出 |

### 前端技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| **Vue.js** | 2.6.10 | 渐进式JavaScript框架,采用MVVM架构,数据驱动视图 |
| **Element UI** | 2.12.0 |饿了么团队开发的UI组件库,提供丰富的企业级组件 |
| **Vuex** | 3.1.1 | Vue官方状态管理模式,集中管理应用状态(如用户信息、菜单) |
| **Vue Router** | 3.0.3 | Vue官方路由管理器,实现单页面应用的路跳转 |
| **Axios** | 0.19.0 | HTTP请求库,基于Promise,用于前后端数据交互 |
| **Font Awesome** | 4.7.0 | 图标库,提供丰富的矢量图标 |
| **SockJS** | - | WebSocket兼容库,在浏览器不支持WebSocket时提供降级方案 |
| **Stomp** | - | STOMP协议实现,用于WebSocket消息传输 |

---

## 🔑 关键代码解析

### 1. 项目启动类 - VhrApplication.java

```java
@SpringBootApplication
@EnableCaching                    // 启用缓存功能,缓存菜单数据提升性能
@MapperScan(basePackages = "org.javaboy.vhr.mapper")  // 扫描MyBatis Mapper接口
@EnableScheduling                 // 启用定时任务,用于邮件发送重试等
public class VhrApplication {
    public static void main(String[] args) {
        SpringApplication.run(VhrApplication.class, args);
    }
}
```

**功能说明**:
- `@SpringBootApplication`: Spring Boot核心注解,组合了@Configuration、@EnableAutoConfiguration、@ComponentScan
- `@EnableCaching`: 启用Spring Cache抽象,配合Redis缓存菜单数据
- `@MapperScan`: 自动扫描Mapper接口,避免每个Mapper都加@Mapper注解
- `@EnableScheduling`: 支持定时任务,如定时检查邮件发送状态并重试

---

### 2. 安全配置类 - SecurityConfig.java

```java
@Configuration
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Bean
    PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();  // 使用BCrypt加密密码
    }

    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http.authorizeRequests()
            .withObjectPostProcessor(new ObjectPostProcessor<FilterSecurityInterceptor>() {
                @Override
                public <O extends FilterSecurityInterceptor> O postProcess(O object) {
                    // 动态权限控制核心配置
                    object.setAccessDecisionManager(customUrlDecisionManager);
                    object.setSecurityMetadataSource(customFilterInvocationSecurityMetadataSource);
                    return object;
                }
            })
            .and()
            .logout()
            .logoutSuccessHandler((req, resp, authentication) -> {
                // 注销成功返回JSON
                resp.setContentType("application/json;charset=utf-8");
                PrintWriter out = resp.getWriter();
                out.write(new ObjectMapper().writeValueAsString(RespBean.ok("注销成功!")));
                out.flush();
                out.close();
            })
            .permitAll()
            .and()
            .csrf().disable().exceptionHandling()
            .authenticationEntryPoint((req, resp, authException) -> {
                // 未认证时返回401状态码和JSON提示
                resp.setContentType("application/json;charset=utf-8");
                resp.setStatus(401);
                PrintWriter out = resp.getWriter();
                RespBean respBean = RespBean.error("访问失败!");
                if (authException instanceof InsufficientAuthenticationException) {
                    respBean.setMsg("请求失败,请联系管理员!");
                }
                out.write(new ObjectMapper().writeValueAsString(respBean));
                out.flush();
                out.close();
            });
    }
}
```

**功能说明**:
- **密码加密**: 使用BCrypt算法加密用户密码,每次加密结果不同,安全性高
- **动态权限控制**: 通过`CustomUrlDecisionManager`和`CustomFilterInvocationSecurityMetadataSource`实现基于数据库的动态权限管理,管理员可随时调整权限无需重启
- **前后端分离适配**: 所有认证失败、注销成功都返回JSON而非重定向,适配前端SPA架构
- **会话管理**: 控制同一用户只能在一个设备登录,防止多设备同时登录

---

### 3. 员工服务类 - EmployeeService.java

```java
@Service
public class EmployeeService {
    @Autowired
    private EmployeeMapper employeeMapper;
    @Autowired(required = false)
    private RabbitTemplate rabbitTemplate;  // RabbitMQ消息队列模板

    // 分页查询员工信息
    public RespPageBean getEmployeeByPage(Integer page, Integer size,
                                          Employee employee, Date[] beginDateScope) {
        if (page != null && size != null) {
            page = (page - 1) * size;  // 计算分页偏移量
        }
        List<Employee> data = employeeMapper.getEmployeeByPage(page, size, employee, beginDateScope);
        Long total = employeeMapper.getTotal(employee, beginDateScope);
        RespPageBean bean = new RespPageBean();
        bean.setData(data);    // 当前页数据
        bean.setTotal(total);  // 总记录数
        return bean;
    }

    // 添加员工并发送欢迎邮件
    public Integer addEmp(Employee employee) {
        // 自动计算合同期限(根据合同开始和结束日期)
        Date beginContract = employee.getBeginContract();
        Date endContract = employee.getEndContract();
        double month = (Double.parseDouble(yearFormat.format(endContract)) -
                       Double.parseDouble(yearFormat.format(beginContract))) * 12
                     + (Double.parseDouble(monthFormat.format(endContract)) -
                       Double.parseDouble(monthFormat.format(beginContract)));
        employee.setContractTerm(Double.parseDouble(decimalFormat.format(month / 12)));

        int result = employeeMapper.insertSelective(employee);
        if (result == 1) {
            enqueueWelcomeMail(employee.getId());  // 异步发送欢迎邮件
        }
        return result;
    }

    // 将欢迎邮件任务加入RabbitMQ队列
    private void enqueueWelcomeMail(Integer employeeId) {
        Employee emp = employeeMapper.getEmployeeById(employeeId);
        String msgId = UUID.randomUUID().toString();
        MailSendLog mailSendLog = new MailSendLog();
        mailSendLog.setMsgId(msgId);
        mailSendLog.setCreateTime(new Date());
        mailSendLog.setExchange(MailConstants.MAIL_EXCHANGE_NAME);
        mailSendLog.setRouteKey(MailConstants.MAIL_ROUTING_KEY_NAME);
        mailSendLog.setEmpId(emp.getId());
        mailSendLog.setTryTime(new Date(System.currentTimeMillis() + 1000L * 60 * MailConstants.MSG_TIMEOUT));
        mailSendLogService.insert(mailSendLog);  // 记录邮件发送日志
        // 发送消息到RabbitMQ
        rabbitTemplate.convertAndSend(MailConstants.MAIL_EXCHANGE_NAME,
                                     MailConstants.MAIL_ROUTING_KEY_NAME, emp,
                                     new CorrelationData(msgId));
    }
}
```

**功能说明**:
- **分页查询**: 实现员工信息的分页展示,减少单次查询数据量,提高性能
- **合同期限计算**: 根据合同开始和结束日期自动计算合同期限(年),避免手动计算错误
- **异步邮件**: 使用RabbitMQ异步发送欢迎邮件,邮件发送失败不影响员工添加,支持重试机制
- **数据统计**: 统计员工总数用于前端分页组件显示总页数

---

### 4. 前端路由配置 - router.js

```javascript
export default new Router({
    routes: [
        {
            path: '/',
            name: 'Login',
            component: Login,
            hidden: true  // 不在菜单中显示登录页
        }, {
            path: '/home',
            name: 'Home',
            component: Home,
            hidden: true,
            meta: {
                roles: ['admin', 'user']  // 权限控制:允许admin和user角色访问
            },
            children: [
                {
                    path: '/chat',
                    name: '在线聊天',
                    component: FriendChat,
                    hidden: true
                }, {
                    path: '/hrinfo',
                    name: '个人中心',
                    component: HrInfo,
                    hidden: true
                }
            ]
        }, {
            path: '*',  // 404页面处理
            redirect: '/home'
        }
    ]
})
```

**功能说明**:
- **路由守卫**: 在main.js中配置路由守卫,实现登录拦截,未登录用户跳转到登录页
- **动态菜单**: 根据用户权限从后端动态加载菜单,不同角色看到不同菜单
- **权限控制**: 通过meta.roles控制页面访问权限,结合Spring Security实现前后端双重权限验证
- **hidden属性**: 标记不在菜单中显示的路由(如登录页、个人中心)

---

### 5. 前端主入口 - main.js

```javascript
// 按需引入Element UI组件,减小打包体积
import {Button, Input, Table, TableColumn, Dialog, Card, Container,
        Icon, Select, Form, Tag, Tree, Pagination, Badge, Loading,
        Message, MessageBox, Menu, Tabs, TabPane, Breadcrumb, ...} from 'element-ui';

// 全局挂载HTTP请求方法,方便在任何组件中调用
Vue.prototype.postRequest = postRequest;
Vue.prototype.putRequest = putRequest;
Vue.prototype.deleteRequest = deleteRequest;
Vue.prototype.getRequest = getRequest;

// 路由守卫 - 登录拦截
router.beforeEach((to, from, next) => {
    if (to.path == '/') {
        next();  // 登录页直接放行
    } else {
        if (window.sessionStorage.getItem("user")) {
            initMenu(router, store);  // 动态初始化菜单
            next();
        } else {
            next('/?redirect=' + to.path);  // 未登录跳转到登录页,并记录原目标路径
        }
    }
})

new Vue({
    router,
    store,
    render: h => h(App)
}).$mount('#app')
```

**功能说明**:
- **按需加载**: 只引入需要的Element UI组件,优化打包体积,避免引入全部组件导致体积过大
- **全局方法**: 将HTTP请求方法挂载到Vue原型,方便在任何组件中通过this.postRequest等方式调用
- **路由守卫**: 实现登录拦截,未登录用户自动跳转到登录页,登录成功后跳回原目标页
- **动态菜单**: 登录成功后调用initMenu动态加载菜单,根据用户权限显示不同菜单

---

## 📦 功能模块详解

### 功能总览表

| 功能模块 | 子功能 | 功能描述 | 技术实现 |
|---------|--------|---------|---------|
| **员工管理** | 基本信息管理 | 员工信息的增删改查,支持分页和高级搜索 | MyBatis分页查询、Spring Boot RESTful API |
| | 员工资料导入 | Excel批量导入员工信息 | Apache POI解析Excel、批量插入数据库 |
| | 员工资料导出 | 导出员工信息到Excel文件 | Apache POI生成Excel、ResponseEntity返回文件流 |
| | 头像上传 | 上传员工头像图片 | FastDFS分布式文件存储、Spring MultipartFile |
| | 合同管理 | 自动计算合同期限 | Java日期计算、SimpleDateFormat格式化 |
| **薪资管理** | 薪资账套管理 | 薪资账套的增删改查(基本工资、奖金、午餐补贴等) | MyBatis CRUD操作 |
| | 员工薪资设置 | 为员工设置对应的薪资账套 | 员工-薪资关联表、MyBatis关联查询 |
| | 工资表生成 | 生成月度工资表 | 定时任务、批量计算工资 |
| | 薪资查询 | 查询历史薪资记录 | 分页查询、日期范围筛选 |
| **系统管理** | 部门管理 | 部门树形结构的增删改查 | 递归查询、树形数据结构 |
| | 职位管理 | 职位信息的增删改查 | 基础CRUD操作 |
| | 职称管理 | 职称信息的增删改查 | 基础CRUD操作 |
| | 权限管理 | 角色、菜单、资源权限管理 | Spring Security动态权限、数据库权限配置 |
| | 操作员管理 | 系统操作员的增删改查、角色分配 | Hr实体、角色关联 |
| | 操作日志 | 记录系统操作日志 | AOP切面、日志表记录 |
| **个人中心** | 个人信息修改 | 修改当前用户信息 | Spring Security获取当前用户、更新操作 |
| | 头像修改 | 上传修改个人头像 | FastDFS文件上传 |
| | 密码修改 | 修改登录密码 | BCrypt加密、密码验证 |
| **在线聊天** | 实时聊天 | WebSocket实时消息推送 | Spring WebSocket、SockJS、Stomp协议 |
| | 在线状态显示 | 显示用户在线/离线状态 | WebSocket连接状态监听 |
| | 消息历史 | 查看聊天历史记录 | 数据库消息表、分页查询 |
| **邮件服务** | 欢迎邮件发送 | 新员工入职自动发送欢迎邮件 | RabbitMQ异步发送、Spring Mail |
| | 邮件发送重试 | 邮件发送失败自动重试 | 定时任务、消息状态检查 |
| | 邉件发送日志 | 记录邮件发送状态和结果 | 数据库日志表、状态字段 |
| **系统配置** | 系统基础配置 | 系统参数配置 | 配置表、缓存优化 |
| | 数据备份 | 数据库备份管理 | mysqldump命令调用 |
| | 系统初始化 | 系统初始化设置 | Flyway数据库迁移 |

---

### 功能模块详细说明

#### 1. 员工管理模块

**核心功能**:
- **员工信息管理**: 包含姓名、性别、出生日期、身份证号、民族、政治面貌、学历、专业、毕业院校、部门、职位、职称、入职日期、合同期限等完整信息
- **高级搜索**: 支持按部门、职位、职称、入职日期范围等多条件组合搜索
- **分页展示**: 采用后端分页,每次只查询当前页数据,提高查询效率
- **Excel导入导出**: 支持批量导入员工信息(减少手动录入工作量),导出员工信息用于备份或报表
- **自动工号生成**: 添加员工时自动生成8位工号(如00000001)

**技术亮点**:
- 使用Apache POI处理Excel,支持大数据量导入导出
- 合同期限自动计算,避免人工计算错误
- 文件上传采用FastDFS分布式存储,支持集群部署

**关键代码**:
```java
// EmpBasicController.java - Excel导出
@GetMapping("/export")
public ResponseEntity<byte[]> exportData() {
    List<Employee> list = (List<Employee>) employeeService
        .getEmployeeByPage(null, null, new Employee(), null).getData();
    return POIUtils.employee2Excel(list);  // 使用POI生成Excel
}

// Excel导入
@PostMapping("/import")
public RespBean importData(MultipartFile file) throws IOException {
    List<Employee> list = POIUtils.excel2Employee(file, ...);  // 解析Excel
    if (employeeService.addEmps(list) == list.size()) {
        return RespBean.ok("上传成功");
    }
    return RespBean.error("上传失败");
}
```

---

#### 2. 薪资管理模块

**核心功能**:
- **薪资账套**: 定义薪资结构,包含基本工资、奖金、午餐补贴、交通补贴、住房补贴、 pension基数、 pension比例、医疗基数、医疗比例、公积金基数、公积金比例等
- **员工薪资设置**: 为每个员工设置对应的薪资账套,支持批量设置
- **工资表生成**: 根据薪资账套和员工信息自动计算月度工资
- **薪资查询**: 查询历史薪资记录,支持按月份、员工筛选

**技术亮点**:
- 薪资账套灵活配置,适应不同企业的薪资结构
- 支持多种社保公积金计算方式
- 工资表自动生成,减少人工计算工作量

**关键代码**:
```java
// SalaryController.java - 薪资账套管理
@RestController
@RequestMapping("/salary/sob")
public class SalaryController {
    @GetMapping("/")
    public List<Salary> getAllSalaries() {
        return salaryService.getAllSalaries();
    }

    @PostMapping("/")
    public RespBean addSalary(@RequestBody Salary salary) {
        if (salaryService.addSalary(salary) == 1) {
            return RespBean.ok("添加成功!");
        }
        return RespBean.error("添加失败!");
    }
}
```

---

#### 3. 系统管理模块

**核心功能**:
- **部门管理**: 支持树形结构的部门管理,包含部门层级关系
- **职位管理**: 管理公司所有职位信息
- **职称管理**: 管理员工职称信息
- **权限管理**: 基于RBAC(角色-权限-资源)模型的权限管理
  - 角色: 定义不同角色(如管理员、普通用户)
  - 菜单: 系统菜单资源,支持树形结构
  - 权限: 为角色分配菜单权限,实现动态权限控制
- **操作员管理**: 管理系统操作员,分配角色
- **操作日志**: 记录所有操作日志,用于审计和追溯

**技术亮点**:
- **动态权限控制**: 权限配置存储在数据库,管理员可随时调整无需重启
- **菜单动态加载**: 前端根据用户权限动态加载菜单,不同角色看到不同菜单
- **Spring Security集成**: 结合Spring Security实现前后端双重权限验证

**关键代码**:
```java
// HrController.java - 操作员管理
@RestController
@RequestMapping("/system/hr")
public class HrController {
    @GetMapping("/")
    public List<Hr> getAllHrs(String keywords) {
        return hrService.getAllHrs(keywords);
    }

    @PutMapping("/role")
    public RespBean updateHrRole(Integer hrid, Integer[] rids) {
        // 更新操作员的角色
        if (hrService.updateHrRole(hrid, rids)) {
            return RespBean.ok("更新成功!");
        }
        return RespBean.error("更新失败!");
    }
}
```

---

#### 4. 在线聊天模块

**核心功能**:
- **实时聊天**: 基于WebSocket实现实时消息推送,支持一对一聊天
- **在线状态显示**: 显示所有用户的在线/离线状态
- **消息历史**: 查看与每个用户的聊天历史记录
- **消息通知**: 新消息实时通知,无需刷新页面

**技术亮点**:
- **WebSocket双向通信**: 服务器可主动推送消息,无需客户端轮询
- **SockJS兼容**: 在浏览器不支持WebSocket时自动降级使用其他技术
- **Stomp协议**: 使用STOMP协议规范消息格式,简化消息处理

**关键代码**:
```java
// WsController.java - WebSocket控制器
@Controller
public class WsController {
    @Autowired
    SimpMessagingTemplate simpMessagingTemplate;

    @MessageMapping("/ws/chat")
    public void handleChat(Principal principal, String chatMsg) {
        // 处理聊天消息,推送给目标用户
        ChatMsg msg = new ChatMsg();
        msg.setFrom(principal.getName());
        msg.setTo(chatMsg.getTo());
        msg.setContent(chatMsg.getContent());
        msg.setDate(new Date());
        // 推送消息到目标用户
        simpMessagingTemplate.convertAndSendToUser(msg.getTo(), "/queue/chat", msg);
    }
}
```

---

#### 5. 邮件服务模块

**核心功能**:
- **欢迎邮件**: 新员工入职时自动发送欢迎邮件
- **异步发送**: 使用RabbitMQ异步发送邮件,不阻塞主业务流程
- **发送重试**: 邹件发送失败时自动重试,提高可靠性
- **发送日志**: 记录每封邮件的发送状态、发送时间、重试次数等

**技术亮点**:
- **RabbitMQ解耦**: 邮件发送独立服务,通过消息队列与主系统解耦
- **消息可靠性**: 记录消息发送日志,支持消息状态追踪和重试
- **定时重试**: 定时任务检查未发送成功的邮件并重试

**关键代码**:
```java
// MailSendTask.java - 定时重试任务
@Component
public class MailSendTask {
    @Scheduled(cron = "0/10 * * * * ?")  // 每10秒执行一次
    public void mailSendTask() {
        List<MailSendLog> logs = mailSendLogService.getMailSendLogsByStatus();
        if (logs.size() == 0) {
            return;
        }
        for (MailSendLog log : logs) {
            if (log.getCount() >= 3) {
                // 重试次数超过3次,标记为发送失败
                mailSendLogService.updateStatus(log.getMsgId(), 2);
            } else {
                // 重新发送邮件
                mailSendLogService.updateCount(log.getMsgId());
                Employee emp = employeeService.getEmployeeById(log.getEmpId());
                rabbitTemplate.convertAndSend(log.getExchange(), log.getRouteKey(), emp,
                    new CorrelationData(log.getMsgId()));
            }
        }
    }
}
```

---

## 🔧 架构特点

### 1. 前后端分离架构

**架构图**:
```
前端(Vue.js) ←→ HTTP/JSON ←→ 后端(Spring Boot)
     ↓                              ↓
  Element UI                    MyBatis
     ↓                              ↓
  Vuex状态管理                   MySQL数据库
     ↓                              ↓
  WebSocket ←→ 实时通信 ←→ WebSocket
```

**优势**:
- 前后端独立开发、独立部署
- 前端采用SPA架构,用户体验流畅
- 后端提供RESTful API,接口标准化
- 支持多端访问(Web、移动端)

---

### 2. 分层架构

**分层结构**:
```
Controller层(控制层)
    ↓ 接收请求、参数校验、调用Service
Service层(业务层)
    ↓ 业务逻辑处理、事务管理、调用Mapper
Mapper层(数据层)
    ↓ 数据库CRUD操作、SQL执行
数据库(MySQL)
```

**优势**:
- 职责清晰,各层专注自己的功能
- 易于维护和扩展
- 支持事务管理,保证数据一致性
- 便于单元测试

---

### 3. 安全机制

**安全架构**:
```
用户登录 → 验证码验证 → Spring Security认证 → BCrypt密码校验
    ↓
生成Session → 权限检查 → 动态权限控制 → 资源访问
    ↓
操作日志记录 → 审计追溯
```

**安全特性**:
- **认证**: 用户名+密码+验证码登录
- **授权**: 基于RBAC模型的动态权限控制
- **密码加密**: BCrypt加密,每次加密结果不同
- **会话管理**: 单设备登录限制
- **CSRF防护**: 禁用CSRF(前后端分离不需要)
- **操作日志**: 记录所有操作,支持审计

---

### 4. 性能优化

**优化措施**:
- **Redis缓存**: 缓存菜单数据,减少数据库查询
- **分页查询**: 后端分页,减少单次查询数据量
- **按需加载**: 前端按需引入Element UI组件,减小打包体积
- **异步处理**: RabbitMQ异步发送邮件,不阻塞主流程
- **连接池**: Druid连接池,优化数据库连接管理

---

## 🚀 部署要点

### 1. 环境准备

**必需环境**:
- JDK 1.8+
- MySQL 5.7+
- Redis 3.0+
- RabbitMQ 3.7+(可选,用于邮件发送)
- Node.js 10+(前端开发)

### 2. 数据库配置

1. 创建数据库:
```sql
CREATE DATABASE vhr CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

2. 导入数据:
```bash
mysql -u root -p vhr < vhr.sql
```

### 3. 后端配置

修改 `vhr-web/src/main/resources/application.yml`:
```yaml
spring:
  datasource:
    username: root          # 数据库用户名
    password: 123456        # 数据库密码
    url: jdbc:mysql://localhost:3306/vhr?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  redis:
    host: localhost         # Redis地址
    port: 6379              # Redis端口
  rabbitmq:
    host: localhost         # RabbitMQ地址
    port: 5672              # RabbitMQ端口
    username: guest         # RabbitMQ用户名
    password: guest         # RabbitMQ密码
```

### 4. 前端部署

**开发环境**:
```bash
cd vuehr
npm install                 # 安装依赖
npm run serve              # 启动开发服务器
```

**生产环境**:
```bash
npm run build              # 打包生成dist目录
# 将dist目录内容部署到Nginx或后端static目录
```

### 5. 启动顺序

1. 启动MySQL数据库
2. 启动Redis服务
3. 启动RabbitMQ(可选)
4. 启动后端: 运行VhrApplication.main()
5. 启动前端: npm run serve(开发)或部署dist(生产)

---

## 📊 数据库设计

### 核心表结构

| 表名 | 说明 | 主要字段 |
|------|------|---------|
| **employee** | 员工信息表 | id, name, gender, birthday, id_card, nation_id, department_id, job_level_id, pos_id, work_id, contract_term等 |
| **department** | 部门表 | id, name, parent_id(树形结构) |
| **position** | 职位表 | id, name, create_date |
| **joblevel** | 职称表 | id, name, title_level_grade |
| **salary** | 薪资账套表 | id, basic_salary, bonus, lunch_salary, traffic_salary, housing_salary, pension_base等 |
| **hr** | 操作员表 | id, name, phone, username, password, enabled |
| **role** | 角色表 | id, name, name_zh |
| **menu** | 菜单表 | id, url, path, component, name, icon_cls, parent_id |
| **menu_role** | 菜单-角色关联表 | id, menu_id, role_id |
| **hr_role** | 操作员-角色关联表 | id, hrid, rid |
| **adjustsalary** | 调薪记录表 | id, eid, as_date, before_salary, after_salary, reason |
| **employeeec** | 员工奖惩记录表 | id, eid, ecdate, ecreason, ecpoint, type |
| **employeetrain** | 员工培训记录表 | id, eid, train_date, train_content, train_result |
| **employeeremove** | 员工调动记录表 | id, eid, removedate, after_department_id, reason |
| **appraise** | 员工考评记录表 | id, eid, appraisedate, appraisecontent, appraisescore |
| **mail_send_log** | 邮件发送日志表 | id, msg_id, emp_id, status, route_key, exchange, count, try_time, create_time |
| **oplog** | 操作日志表 | id, hrid, operation, method, params, ip, create_date |

---

## 🎯 项目特色

### 1. 动态权限管理

**传统方案**: 权限配置写在代码或配置文件中,修改需要重启

**本项目方案**: 权限配置存储在数据库,管理员可随时调整,系统自动加载最新权限

**实现原理**:
- Spring Security的`FilterSecurityInterceptor`支持自定义权限元数据源
- `CustomFilterInvocationSecurityMetadataSource`从数据库加载URL-角色映射
- `CustomUrlDecisionManager`根据用户角色判断是否有权限访问URL

---

### 2. 前后端分离适配

**传统方案**: Spring Security登录失败重定向到登录页,不适合前后端分离

**本项目方案**: 所有认证失败、注销成功都返回JSON,前端根据状态码处理

**实现原理**:
- 自定义`LoginFilter`处理登录请求,成功/失败都返回JSON
- 自定义`AuthenticationEntryPoint`处理未认证请求,返回401状态码和JSON
- 自定义`LogoutSuccessHandler`处理注销成功,返回JSON

---

### 3. 消息队列异步处理

**传统方案**: 邮件发送在主线程执行,发送失败影响业务流程

**本项目方案**: 邮件发送通过RabbitMQ异步处理,发送失败不影响业务

**实现原理**:
- 主系统将邮件任务发送到RabbitMQ队列
- 独立的mailserver服务监听队列并发送邮件
- 记录邮件发送日志,定时任务检查并重试失败邮件

---

### 4. WebSocket实时通信

**传统方案**: 使用HTTP轮询实现实时聊天,效率低、延迟高

**本项目方案**: 使用WebSocket实现双向实时通信,效率高、延迟低

**实现原理**:
- Spring Boot内置WebSocket支持
- 使用SockJS提供WebSocket兼容方案
- 使用STOMP协议规范消息格式
- 前端订阅消息通道,实时接收推送消息

---

## 📝 学习价值

### 1. 技术栈全面

本项目涵盖了现代企业级应用开发的主流技术栈:
- **后端**: Spring Boot、MyBatis、Spring Security、WebSocket、RabbitMQ、Redis
- **前端**: Vue.js、Element UI、Vuex、Vue Router、WebSocket
- **数据库**: MySQL、Redis
- **文件存储**: FastDFS
- **消息队列**: RabbitMQ

适合学习前后端分离开发、微服务架构、分布式系统等技术。

---

### 2. 业务场景真实

本项目模拟真实的人力资源管理系统,包含:
- 员工管理、薪资管理、权限管理等常见业务模块
- Excel导入导出、文件上传、邮件发送等实用功能
- 动态权限、实时聊天、异步处理等技术难点

适合学习企业级应用的业务逻辑处理和架构设计。

---

### 3. 代码质量高

本项目代码结构清晰、注释完整、命名规范:
- 采用分层架构,职责清晰
- 使用设计模式,代码复用性强
- 异常处理完善,用户体验友好
- 安全机制健全,防止常见攻击

适合学习高质量代码的编写规范和最佳实践。

---

## 🔗 相关资源

- **GitHub**: https://github.com/lenve/vhr
- **Gitee**: https://gitee.com/lenve/vhr
- **作者博客**: http://www.javaboy.org
- **部署教程**: [前后端分离项目常见部署问题汇总](https://mp.weixin.qq.com/s/dcJv6BIVFPnokI8nBf4IrQ)

---

## 📌 总结

微人事(VHR)是一个功能完善、技术先进、架构合理的人力资源管理系统。项目采用前后端分离架构,后端基于Spring Boot提供RESTful API,前端基于Vue.js构建SPA应用。系统包含员工管理、薪资管理、权限管理、在线聊天、邮件服务等核心功能模块,采用动态权限控制、WebSocket实时通信、RabbitMQ异步处理等技术亮点。项目代码质量高,适合学习现代企业级应用开发技术栈和最佳实践。

---

**文档生成时间**: 2026-06-21
**项目版本**: vhr-master
**分析工具**: Trae IDE